/* ===========================================================
   Liel Bridal Widgets — shared frontend JS
   Per-section behaviour (carousels, etc.) added as we build.
   =========================================================== */
( function () {
  'use strict';
  // Section scripts are appended below as we build each widget.
}() );
